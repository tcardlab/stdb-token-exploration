import { Compiler } from "./compiler.js";
import { File } from "./file.js";
import { SiteProcessor } from "./ssp.js";
export declare class Runtime {
    #private;
    files: Map<string, File>;
    handlers: Map<string, (body: string) => string>;
    compiler: Compiler;
    constructor(config?: {
        siteDir?: string;
        processor?: SiteProcessor;
        jsxContentSsg?: string | Buffer;
        jsxContentBrowser?: string | Buffer;
    });
    build(): any;
    rebuildAll(): void;
    pathsUpdated(...paths: string[]): void;
    realPathFor(filepath: string): string;
    addDeps(requiredBy: string, requiring: string): void;
}
